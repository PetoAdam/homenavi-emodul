apiVersion: v1
kind: Service
metadata:
  name: {{ include "emodul.fullname" . }}
  labels:
    {{- include "emodul.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "emodul.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
